import 'dart:io';

void main() {


// Mengapa dart ?
// 1. Dart merupakan bahasa yang dikembangkan Google, awal pada tahun 2013,
// Dart memungkinkan kita untuk membuat Program yang dapat berjalan di berbagai
// Platform, seperti Windows, IOS, linux dan sebagainya, untuk pengembangan game, server side, beck end, front end web, desktop dan mobile

// 2. Dart mudah dipahami untuk pemula yang belum pernah belajar pemrograman
// dan bagi yang pernah belajar bahasa pemrograman bisa memperdalam OOP, functional
// dan berbagai paradigma pemrograman lainnya

// 3. Pengembangan yang produktif karena didukung hot reload
// Optimasi UI, karena menggunakan Widget2 yang disediakan dart, mudah di kostumisasi
// terkhusus menggunakan framework flutter, dengan bahasa yang mudah dipahami

// instalasi langsung download Dart sdk, jika menggunakan VS code bisa langsung
// install ekstensi Dart

  // print("Harga Satuan Permen :");
  // int a = int.parse(stdin.readLineSync());

  // print("-------Bonus------");
  // print("Setiap .... Bungkus :");
  // int b = int.parse(stdin.readLineSync());

  // print("Ditukar Menjadi ... permen :");
  // int c = int.parse(stdin.readLineSync());

  // print("-------Uang------");
  // print("Harga Uang Jajan :");
  // int d = int.parse(stdin.readLineSync());

  // double totalpermen = d / a;

  // double bonus = (totalpermen / b) / c;

  // double totalpermensekarang = totalpermen + bonus;
  // int totalpermensekarang1 = totalpermensekarang.toInt();

  // print(totalpermensekarang1);
}
